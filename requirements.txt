# Tele2Rubika - runtime dependencies
# Keep this list small: the bot runs inside GitHub Actions on every handoff.

telethon>=1.36.0,<2.0.0
httpx>=0.27.0,<1.0.0
PyYAML>=6.0,<7.0

# --- local development / one-off scripts only ---
python-dotenv>=1.0.0,<2.0.0
